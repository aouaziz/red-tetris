{
  "name": "red-tetris",
  "version": "1.0.0",
  "private": true,
  "description": "Networked multiplayer Tetris — full stack JavaScript/TypeScript",
  "scripts": {
    "dev": "export PATH=\"$PWD/.node/bin:$PATH\" && npm run build && npm start",
    "dev:server": "export PATH=\"$PWD/.node/bin:$PATH\" && tsx watch server/src/index.ts",
    "dev:client": "export PATH=\"$PWD/.node/bin:$PATH\" && vite",
    "build": "npm run build:client && npm run build:server",
    "build:client": "export PATH=\"$PWD/.node/bin:$PATH\" && vite build",
    "build:server": "export PATH=\"$PWD/.node/bin:$PATH\" && tsc -p tsconfig.server.json",
    "start": "export PATH=\"$PWD/.node/bin:$PATH\" && node dist/server/index.js",
    "test": "export PATH=\"$PWD/.node/bin:$PATH\" && vitest run",
    "test:coverage": "export PATH=\"$PWD/.node/bin:$PATH\" && vitest run --coverage",
    "lint": "export PATH=\"$PWD/.node/bin:$PATH\" && eslint .",
    "typecheck": "export PATH=\"$PWD/.node/bin:$PATH\" && tsc -p tsconfig.server.json --noEmit && tsc -p client/tsconfig.json --noEmit"
  },
  "dependencies": {
    "express": "^4.19.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2",
    "socket.io": "^4.8.0",
    "socket.io-client": "^4.8.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.11.1",
    "@types/express": "^4.17.21",
    "@types/node": "^20.16.10",
    "@types/react": "^18.3.11",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.2",
    "@vitest/coverage-v8": "^2.1.2",
    "eslint": "^9.11.1",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2",
    "typescript-eslint": "^8.8.0",
    "vite": "^5.4.8",
    "vitest": "^2.1.2"
  },
  "allowScripts": {
    "esbuild@0.28.2": true,
    "esbuild@0.21.5": true
  }
}
